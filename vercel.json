{
  "build": {
    "env": {
      "NODE_VERSION": "10"
    },
    "command": "npm install -g n && n 10 && npm install -g gitbook-cli && gitbook install && gitbook build"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/_book/$1"
    }
  ]
}
